PRoGram Evaluacion;
uses CRT;

writeln('hola penagos');
fUnCtion evaluar (a, b : integer; c: real):integer;
  var
    p,q : integer;
    aV, av: char;
    r : integer = 100;
    qEw9 : ^integer;
    r : real;
    z : ^char;
    val : Boolean = true;
    nom : string;
    //este es un comentario
    begin
      q := p;
      if (a>0) then
        begin
          p = a + 1;
        end
      else
        begin
          q = b;    ;
          if (b>0) then
            begin
              p = 1;
              while p <= 100 do
                begin
                  q = q + 1;
                  Dec(r, 1);
                end
            end
          else
            begin
              for p = 0 to 99 do
                begin
                  c = c + 1;
                end
              querido compañero programador del futuro, los comentarios de varias lineas
              debe ser: {ponga lo que quiera}. entendido?
              se le quiere y se le respeta}
            end
      a := b;

      case a of
        '1' : a = b;
        '2' : a = c;
        '3' : c = b + c;
      else
        begin
          a := 0;
        end
      evaluar := a + 1;
    end
function fibonaci (i : integer):integer;
  if ( i := 0) then
    begin
      fibonaci := 0;
    end
  if ( i := 1) then
    begin
      fibonaci := 1;
    end
  fibonaci := fibonaci(i-1) + fibonaci(i-2);
  readln;
 
